void main(){
  int? age;
  print(age);
  // The variable 'age' must be initialized before it can be used.
  //add question mark to make it nullable


  String? name;
  print(name);
  // The variable 'name' must be initialized before it can be used.
  //add question mark to make it nullable
}